# ── Egyptian NID OCR — Flask API requirements ─────────────────────────────────
# Install: pip install -r requirements.txt
# Python 3.10+ required

# ── Web framework ─────────────────────────────────────────────────────────────
flask>=3.0.3
flask-cors>=4.0.1
python-dotenv>=1.0.1

# ── LLM providers (at least one API key required for fast mode) ───────────────
# OpenRouter / Google AI Studio via OpenAI-compatible SDK
openai>=1.0.0
# Google Gemini direct SDK (fallback)
google-genai>=1.0.0

# ── Image processing ──────────────────────────────────────────────────────────
# Use headless variant on servers (no display required)
opencv-python-headless>=4.10.0
numpy>=2.0.0
Pillow>=10.0.0

# PaddleOCR removed — too large for cloud deployment (multi-GB download)
# LLM-based extraction (Gemini/Groq) handles all NID fields
